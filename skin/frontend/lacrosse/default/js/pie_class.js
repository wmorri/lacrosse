$(function() {
    if (window.PIE) {
	//$('.thum_img').each(function(){PIE.attach(this);});
	//$('.external img').each(function(){PIE.attach(this);});
	// $('.cycle-pager-active').each(function(){PIE.attach(this);});
    }
});